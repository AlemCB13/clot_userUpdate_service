version: '3.8'
services:
  app:
    build: .
    ports:
      - "3002:3002"
    env_file:
      - .env
    networks:
      - clot_usercreate_service_default

networks:
  clot_usercreate_service_default:
    external: true
